export enum ButtonType {
  primary = "primary",
  secondary = "secondary",
  tertiary = "tertiary",
}

export enum BannerInfoType {
  success = "success",
  warning = "warning",
  error = "error",
  info = "info",
  none = "none",
}
